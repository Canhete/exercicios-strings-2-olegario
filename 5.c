#include <stdio.h>

int ehPalindromo(char *string){
    char inicio = 0;
    char fim = strlen(string - 1);
    while(inicio >= fim){
        if((string[inicio] || string[fim]))
        if((string[inicio] == string[fim])){
            
        }
        
        inicio++;
        fim--;
    }
}

void main(){

}