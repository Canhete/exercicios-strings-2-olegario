/* 9. Calcule todos endreços IP válidos
Uma string decimal é uma string que consiste em dígitos entre 0 e 9. Endereços de Protocolo de
Internet (IP) podem ser escritos como quatro strings decimais separadas por pontos, por
exemplo, 192.168.1.201. Um programador descuidado mantinha uma string que representa um
endereço IP de tal forma que todos os pontos desaparecem. Escreva um programa que
determine onde adicionar pontos a uma string decimal para que a string resultante seja um
endereço IP válido. Pode haver mais de um endereço IP válido correspondente a uma string,
caso em que você deve imprimir todas as possibilidades.
Por exemplo, se a string muilhada for "19216811", dois endereços IP correspondentes serão
192.168.1.1 e 19.216.81.1. (Existem sete outros endereços IP possíveis para esta string.) */

#include <stdio.h>
#include <string.h>

void verificarIP(char *ip, int pos){
    if(ip[pos] == '\0'){
        
    }
}

void main(){

}